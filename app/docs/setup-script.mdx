export const metadata = {
  metadataBase: new URL("https://ui-layouts.com"),

title: "Setup Script | Obfus-Tail",
description:
"Simple 3-step setup for Tailwind class obfuscation",
keywords: [
"Obfus-Tail setup script",
"React components setup",
"Next.js Tailwind guide",
"Install UI components",
"Obfus-Tail documentation",
"How to use Obfus-Tail",
"Component library setup",
"React UI getting started",
"TailwindCSS component guide",
"Next.js UI tutorial",
],
};

# Setup Script

Simple 3-step setup to get Tailwind class obfuscation working in your project.

## Step 1: Add the Script

Create `scripts/obfuscate-tailwind.js`:

```javascript
const fs = require('fs');
const path = require('path');

// Configuration
const config = {
  sourceDir: './app',
  outputFile: './app/obfuscated-styles.css',
  mapFilePath: '.obfuscation-map.json',
  randomNameLength: 8,
};

// Simple class obfuscator
function obfuscateClass(className) {
  const chars = 'abcdefghijklmnopqrstuvwxyz0123456789';
  let result = '';
  for (let i = 0; i < config.randomNameLength; i++) {
    result += chars.charAt(Math.floor(Math.random() * chars.length));
  }
  return result;
}

// Process files
function processFiles() {
  const classMap = {};
  let css = '@import "tailwindcss";\n';
  
  // Add token reference if exists
  if (fs.existsSync('./app/token.css')) {
    css += '@import "./token.css";\n';
  }
  css += '\n';
  
  // Find all TSX/JSX files
  function findFiles(dir) {
    const files = [];
    const items = fs.readdirSync(dir);
    
    for (const item of items) {
      const fullPath = path.join(dir, item);
      const stat = fs.statSync(fullPath);
      
      if (stat.isDirectory() && !item.startsWith('.') && item !== 'node_modules') {
        files.push(...findFiles(fullPath));
      } else if (item.match(/\.(tsx|jsx|ts|js)$/)) {
        files.push(fullPath);
      }
    }
    return files;
  }
  
  const files = findFiles(config.sourceDir);
  
  // Process each file
  for (const filePath of files) {
    let content = fs.readFileSync(filePath, 'utf8');
    
    // Find all className attributes
    const classRegex = /className\s*=\s*["']([^"']+)["']/g;
    let match;
    
    while ((match = classRegex.exec(content)) !== null) {
      const originalClasses = match[1];
      const obfuscatedClasses = [];
      
      for (const className of originalClasses.split(' ')) {
        if (className && !classMap[className]) {
          const obfuscated = obfuscateClass(className);
          classMap[className] = obfuscated;
          
          // Generate CSS for this class
          css += `.${obfuscated} {\n`;
          css += `  @apply ${className};\n`;
          css += `}\n\n`;
        }
        
        if (classMap[className]) {
          obfuscatedClasses.push(classMap[className]);
        }
      }
      
      // Replace in content
      content = content.replace(match[0], `className="${obfuscatedClasses.join(' ')}"`);
    }
    
    // Write back file
    fs.writeFileSync(filePath, content);
  }
  
  // Write CSS file
  fs.writeFileSync(config.outputFile, css);
  
  // Write map file
  fs.writeFileSync(config.mapFilePath, JSON.stringify(classMap, null, 2));
  
  console.log(`✅ Processed ${files.length} files`);
  console.log(`📄 Generated ${config.outputFile}`);
  console.log(`🗺️  Saved class map to ${config.mapFilePath}`);
}

// Run the script
processFiles();
```

## Step 2: Update Build Script

Add the obfuscation step to your `package.json`:

```json
{
  "scripts": {
    "build": "node scripts/obfuscate-tailwind.js && next build"
  }
}
```

## Step 3: Create Token File (Optional)

Create `app/token.css` - this is just your styles, nothing special:

```css
/* Your theme variables - simple as that */
:root {
  --background: oklch(1 0 0);
  --foreground: oklch(0.145 0 0);
  --primary: oklch(0.205 0 0);
  --primary-foreground: oklch(0.985 0 0);
  --border: oklch(0.922 0 0);
}

@theme inline {
  --color-background: var(--background);
  --color-foreground: var(--foreground);
  --color-primary: var(--primary);
  --color-primary-foreground: var(--primary-foreground);
  --color-border: var(--border);
}
```

Then import it in `app/globals.css`:

```css
@import "tailwindcss";
@import "./token.css";
```

## That's It! 🎉

Now run your build:

```bash
npm run build
```

**What happens:**
1. Script finds all `className` attributes
2. Generates random class names (like `a1b2c3d4`)
3. Creates CSS file with `@apply` rules
4. Updates your files with obfuscated classes
5. Saves a map file for reference

**Example:**
- Before: `className="bg-primary text-white p-4"`
- After: `className="x7y9z2w1 q3m5n6p8"`
- CSS: `.x7y9z2w1 { @apply bg-primary; }`

**Don't panic about the token file** - it's just your regular CSS variables. The script automatically detects and includes it if it exists.

That's all you need for Tailwind class obfuscation! 🚀
